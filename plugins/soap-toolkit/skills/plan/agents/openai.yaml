interface:
  display_name: "Plan"
  short_description: "Clarify requirements and write a grounded implementation plan"
  default_prompt: "Use $plan to inspect the current implementation, clarify unresolved requirements, and write a focused plan."

policy:
  allow_implicit_invocation: false
