interface:
  display_name: "Plan"
  short_description: "Inspect, discuss, and create a minimal implementation plan"
  default_prompt: "Use $plan to inspect the current implementation and create a focused plan."
